<h1>Today's Posts</h1>

<%= link_to 'New Blog Post', new_post_path %>



<div>
  <% @posts.each do |post| %>
    <h2><%= link_to post.title, show_path(id: post.id) %></h2>
    <p><%= post.body%></p>
  <% end %>
</div>
